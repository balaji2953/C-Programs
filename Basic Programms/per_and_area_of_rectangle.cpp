#include<stdio.h>
#include<conio.h>
int main()
{
	int height=7,width=5;
	float per,area;
	
	per=2*(height+width);
	area=height*width;
	
	printf("perameter of the rectangle=%f",per);
	printf("Area of the rectangle=%f",area);
	
	return 0;

	
}